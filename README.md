# DailyexpenseApp
Daily Expense App is an Android app to record daily expenses with amount, description, category (Spinner), and payment mode (RadioButtons). It displays expenses in a list and allows call, SMS, and email with confirmation dialog.
